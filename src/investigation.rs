use thirtyfour::{DesiredCapabilities, WebDriver};

mod google_search;
mod wikidata;
// pub async fn start_investigation() {
//     let caps = DesiredCapabilities::chromium();
//     let driver = WebDriver::new("http://localhost:4444", &caps).await.unwrap();
// }